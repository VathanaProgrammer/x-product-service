server:
  port: ${PRODUCT_SERVICE_PORT:8082}

spring:
  application:
    name: Vyntra-Product-Service
  profiles:
    default: local

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:86400000}
